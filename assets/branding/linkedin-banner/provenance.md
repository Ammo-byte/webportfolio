# LinkedIn Banner Provenance

The background was generated with the built-in image-generation workflow as a sparse near-black pixel field with a restrained blue signal path and no text or logo.

The final 1584 × 396 PNG was cropped deterministically and composited with the existing `logo-dark.png` asset using nearest-neighbor scaling so the approved AA mark remains unchanged and pixel-crisp.
